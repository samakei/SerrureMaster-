###############################################
# Exemple d’environnement pour Vite
#
# Copiez en .env.local (dev) et .env.production.local
# (build/preview) puis renseignez des valeurs réelles.
# Redémarrez toujours `npm run dev` après modification.
#
# Rappels:
# - Vite n’expose que les variables préfixées par VITE_
# - Ne mettez JAMAIS de secrets serveur (service role) côté client
# - Ordre de chargement:
#   Dev: .env → .env.local
#   Build/Preview: .env → .env.production → .env.production.local
###############################################

# Supabase (Project Settings → API)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Stripe (clé publique, côté client)
VITE_STRIPE_PUBLIC_KEY=

# Gemini (optionnel)
VITE_GEMINI_API_KEY=

# CI/CD (Staging Cloud Run):
# Ajoutez aussi ces variables dans GitHub → Settings → Actions → Variables
# Requis: WORKLOAD_IDENTITY_PROVIDER, GCP_PROJECT_ID, GCP_SERVICE_ACCOUNT_EMAIL,
#         VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, VITE_STRIPE_PUBLIC_KEY
# Optionnel: VITE_GEMINI_API_KEY
