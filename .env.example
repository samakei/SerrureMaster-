# Copiez ce fichier en ".env.local" et renseignez des valeurs réelles.
# Ne mettez JAMAIS de secrets serveur ici (clés privées, tokens admin).

# Supabase (obligatoire)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Stripe (clé publique obligatoire)
VITE_STRIPE_PUBLIC_KEY=

# Gemini (optionnel)
VITE_GEMINI_API_KEY=

# Conseils:
# - Utilisez les mêmes variables dans les Variables du dépôt GitHub pour le staging.
#   GITHUB → Settings → Variables → Repository variables
#   Requis: WORKLOAD_IDENTITY_PROVIDER, GCP_PROJECT_ID, GCP_SERVICE_ACCOUNT_EMAIL,
#           VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, VITE_STRIPE_PUBLIC_KEY
#   Optionnel: VITE_GEMINI_API_KEY
# - En local: créez ".env.local" à la racine et relancez `npm run dev`.
# Copiez ce fichier en .env.local et renseignez les vraies valeurs
# Vite n’expose que les variables préfixées par VITE_

# Supabase (Project Settings → API)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Stripe (clé publique, côté client)
VITE_STRIPE_PUBLIC_KEY=

# Gemini (optionnel)
VITE_GEMINI_API_KEY=
