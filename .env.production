# Production environment for Vite build
# Remplacez ces valeurs par vos vraies clés/URLs de production

# Frontend runtime values (PUBLIC)
VITE_ENV=production
VITE_APP_URL=https://serruremaster.com

# Supabase (PUBLIC anon key)
VITE_SUPABASE_URL=https://<your-supabase-project>.supabase.co
VITE_SUPABASE_ANON_KEY=<your-anon-key>

# Stripe (PUBLIC key)
VITE_STRIPE_PUBLIC_KEY=pk_live_<your_public_key>

# Gemini (si utilisé côté client)
VITE_GEMINI_API_KEY=<your_public_key_or_remove_if_server_only>

# NOTE: NE PAS mettre de clés secrètes (sk_live_...) dans ce fichier.
# Les secrets doivent rester côté backend (Edge Functions / Cloud Run APIs).
